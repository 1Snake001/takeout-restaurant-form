/*
import sqlite3 from 'sqlite3';
let db = new sqlite3.Database('./db/orders.db', (err) => {
  if (err) {
    console.error(err.message);
  }
  console.log('Connected to the orders database.');
});

 */
const db = {};
export default db;
